#if not defined(RESET_HEADER_FILE)
#define RESET_HEADER_FILE

/**
 * Link undocumented mbed reset function. Equal to pressing the little button
 * on top of the board.
 */
extern "C" void mbed_reset();

#endif

