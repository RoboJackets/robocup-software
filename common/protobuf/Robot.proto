package Packet;

import "Control.proto";
import "Tuning.proto";

message Robot
{
    enum TxType
    {
        CONTROL  = 1;
        TUNING   = 2;
    }

    required uint32 uid = 1;
    required TxType txType = 2;
    required Control control = 3;
    optional Tuning tuning = 4;
}