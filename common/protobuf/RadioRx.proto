package Packet;

// Packet going out from radio program to control program
message RadioRx
{
	required uint64 timestamp = 1;
	required uint32 board_id = 2;
	required float rssi = 3;
	required float battery = 4;
	
	// ball sensor
	required bool ball = 5;
	
	// True if the kicker is fully charged
	required bool charged = 6;
	
	repeated bool motorFault = 7;

	repeated uint32 encoders = 8;
}
