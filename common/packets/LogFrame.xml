<struct typename="LogFrame">
	<struct typename="Vision" include_cpp="Vision.hpp" external_cpp="Packet::Vision" external_python="Packet.Vision" />
	<struct typename="Point2d" include_cpp="Geometry/Point2d.hpp" external_cpp="Geometry::Point2d" external_python="Geometry.Point2d" />
	<struct typename="RadioTx" include_cpp="RadioTx.hpp" external_cpp="Packet::RadioTx" external_python="Packet.RadioTx"/>
	<struct typename="RadioRx" include_cpp="RadioRx.hpp" external_cpp="Packet::RadioRx" external_python="Packet.RadioRx"/>

    <!-- see Team.h -->
    <enum type="int8" typename="Team" external_cpp="Team" include_cpp="Team.h">
        <constant name="UnknownTeam" value="-1"/>
        <constant name="Yellow" value="0"/>
        <constant name="Blue" value="1"/>
    </enum>

	<enum type="uint8" typename="RunState">
        <constant name="Running"/>
        <constant name="Stopped"/>
    </enum>
    
    <enum type="uint8" typename="ControlState">
        <constant name="Manual"/>
        <constant name="Auto"/>
    </enum>

    <!-- Game state as determined by referee -->
    <struct typename="GameState">
        <enum type="uint8" typename="Period">
            <constant name="FirstHalf"/>
            <constant name="Halftime"/>
            <constant name="SecondHalf"/>
            <constant name="Overtime1"/>
            <constant name="Overtime2"/>
            <constant name="PenaltyShootout"/>
        </enum>
        
        <enum type="uint8" typename="State">
            <constant name="Halt"/>         <!-- Robots must not move -->
            <constant name="Stop"/>         <!-- Robots must stay 500mm away from ball -->
            <constant name="Start"/>        <!-- Robots not on starting team must stay 500mm away from ball -->
            <constant name="Ready"/>        <!-- A robot on the starting team may kick the ball -->
            <constant name="Playing"/>      <!-- Normal play -->
        </enum>
        
        <!-- Types of restarts -->
        <enum type="uint8" typename="Restart">
            <constant name="None"/>
            <constant name="Kickoff"/>
            <constant name="Direct"/>
            <constant name="Indirect"/>
            <constant name="Penalty"/>
        </enum>
        
        <member type="Period" name="period"/>
        <member type="State" name="state"/>
        <member type="Restart" name="restart"/>
        
        <!-- Which team can kick the ball during a restart -->
        <member type="Team" name="startingTeam"/>
        
        <!-- Scores -->
        <member type="uint8" name="scoreYellow"/>
        <member type="uint8" name="scoreBlue"/>
        
        <!-- Time in seconds remaining in the current period -->
        <member type="uint16" name="secondsRemaining"/>
    </struct>
    
    <!-- Commands for motion -->
    <struct typename="MotionCmd">
        <!--Goal position for the robot-->
        <member type="Point2d" name="goal" />

        <!-- motion generated commanded position of the robot -->
        <member type="Point2d" name="pos" />

        <!-- feedforward velocity of the robot -->
        <member type="Point2d" name="v_ff" />

        <!-- commanded angle of the robot (+- 180 degrees) -->
        <member type="float" name="angle" />

        <!-- The velocity command to the robot in team space -->
        <member type="Point2d" name="vel" />
    </struct>

	<!-- post process robot information -->
	<struct typename="Robot">
		<!-- shell id, used for debug -->
		<member type="uint8" name="shell" />

		<!-- position of the robot -->
		<member type="Point2d" name="pos" />

		<!-- velocity of the robot -->
		<member type="Point2d" name="vel" />

		<!-- angle of the robot (+- 180 degrees) -->
		<member type="float" name="angle" />

		<!-- angular velocity of the robot degrees/timestep -->
		<member type="float" name="angleVel" />

        	<!-- MotionCmd Structure -->
        	<member type="MotionCmd" name="cmd" />

		<!-- if true, this robot is active for use -->
		<member type="bool" name="valid" value="false" />
		
		<!-- radio tx packet for the robot -->
		<member type="RadioTx::Robot" name="radioTx" />
		
		<!-- radio rx packet for the robot -->
		<member type="RadioRx::Robot" name="radioRx" />
	</struct>

	<!-- post processed ball information -->
	<struct typename="Ball">
		<member type="Point2d" name="pos" />
		<member type="Point2d" name="vel" />

		<member type="bool" name="valid" value="false" />
	</struct>
	
	<!-- timestamp of the vision packet that clocked the system -->
	<member type="uint64" name="timestamp" />

	<!-- team we are running as -->
    <member type="Team" name="team" />

	<member type="ControlState" name="controlState" value="Manual" />
    <member type="RunState" name="runState" value="Stopped" />

	<!-- unprocessed vision, in team space -->
	<array type="Packet::Vision" name="rawVision" />

	<member type="GameState" name="gameState"/>

	<!-- array of robots for the system, index is player id -->
	<array size="5" type="Robot" name="self"/>
	<array size="5" type="Robot" name="opp"/>

	<!-- gameplay ball -->
	<member type="Ball" name="ball" />

    <array type="Point2d" name="pathTest"/>
    <struct typename="Segment" external_cpp="Geometry::Segment" include_cpp="Geometry/Segment.hpp">
        <array size="2" type="Point2d" name="pt"/>
    </struct>
    <array type="Segment" name="rrt"/>
</struct>
