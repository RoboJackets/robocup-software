<simulation>
	<!-- testPass.xml: this is a test scenario for passing where
		 the blue team has a situation where passing should be
		 performed.
		 The yellow team's defender is blocking the line of sight
		 from blue robot #1, and blue robot #0 has a better shot-->
	
    <!-- if this line is not present, there will be no ball -->
    <ball x="0" y="-1.4" />

    <field />

    <!-- configure blue team robots -->
    <blue>
        <robot id="0" x="0" y="-1.5" />
        <robot id="1" x="-1" y="1.5" />
        <!-- <robots qty="4" />-->
        <kinematics />
    </blue>

    <yellow>
        <robot id="0" x="-1.5" y="-1" />
    </yellow>

    <vision>
        <!-- noise, fps, dropout model -->
    </vision>

    <radio>
        <!-- packet loss rate -->
    </radio>

</simulation>
