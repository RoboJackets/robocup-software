import { Element, Node } from '@xmldom/xmldom';
import { default as Pose } from '../math/Pose.ts';
export declare function parseUrdfOrigin(originElement: Element): Pose;
export declare function isElement(node: Node): node is Element;
