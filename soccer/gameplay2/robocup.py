# this is a dummy file here for testing purposes
# plays, etc require the 'robocup' module which is normally provided by `soccer` at runtime
# TODO: delete this file and have scons build us a `robocup.so` file that's identically the module that's loaded at runtime in `soccer`
